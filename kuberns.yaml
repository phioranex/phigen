install:
  - npm ci

build:
  - npx prisma generate
  - npm run build
  - npx prisma migrate deploy || true

deploy:
  web:
    command: HOSTNAME=0.0.0.0 node .next/standalone/server.js
    port: 3000
